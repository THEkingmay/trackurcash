# 1. Dependencies (ห้ามเอาจากเครื่องเราไปเด็ดขาด!)
node_modules
pnpm-lock.yaml.tmp

# 2. Build Artifacts (ห้ามเอาโฟลเดอร์ build ที่เคยทำในเครื่องไปปน)
.next
out
build
dist

# 3. Environment Secrets (ความลับห้ามหลุดไปใน Image)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 4. Debug Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 5. Version Control
.git
.gitignore

# 6. OS Metadata
.DS_Store
Thumbs.db

# 7. Docker Files (ตัวมันเองไม่ต้องเข้าไปใน Image ก็ได้ค่ะ)
Dockerfile
docker-compose.yml
.dockerignore